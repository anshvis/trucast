Make sure that all Python Libraries are installed (check main.py file)

How to run locally: python3 main.py

How to download (macOS/Linux):

- python3 setup.py py2app

Windows:

- pyinstaller --onefile --add-data --windowed "insert full folder path for prophet;./prophet" main.py
